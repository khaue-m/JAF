<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JAF Transport - Mudanças e Transportes</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=League+Spartan:wght@400;700&display=swap" rel="stylesheet">

    <style>
        /* Cores da marca */
        :root {
            --primary-color: #000100; /* Preto */
            --secondary-color: #ffb111; /* Laranja */
            --background-color: #fffff1; /* Off-white */
            --text-color: #333;
        }

        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--background-color);
            color: var(--text-color);
        }

        /* Aplica a fonte League Spartan apenas nos títulos */
        h1, h2 {
            font-family: 'League Spartan', sans-serif;
            font-weight: 700; /* Usa a variação em negrito da fonte */
        }
        
        header {
            background-color: var(--primary-color);
            color: #fff;
            padding: 20px 0;
            text-align: center;
        }

        .logo {
            width: 100px; /* Ajuste o tamanho do logo conforme necessário */
            margin-bottom: 10px;
        }

        header h1 {
            margin: 0;
            font-size: 2em;
        }

        nav {
            display: flex;
            justify-content: center;
            gap: 20px;
            background-color: #333; /* Cor mais escura para o menu */
            padding: 10px 0;
        }

        nav a {
            color: #fff;
            text-decoration: none;
            font-weight: bold;
            transition: color 0.3s;
        }

        nav a:hover {
            color: var(--secondary-color);
        }

        section {
            padding: 50px 20px;
            max-width: 1200px;
            margin: 0 auto;
        }

        h2 {
            text-align: center;
            margin-bottom: 30px;
            color: var(--primary-color);
        }

        /* Home */
        .banner {
            background: url('https://images.unsplash.com/photo-1556910243-524f4639e4a3?q=80&w=2070&auto=format&fit=crop') no-repeat center center/cover;
            height: 400px;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #fff;
            font-size: 2em;
            font-weight: bold;
            text-shadow: 2px 2px 5px var(--primary-color);
        }
        
        /* Formulário */
        form {
            background-color: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 0px 15px rgba(0,0,0,0.1);
            max-width: 600px;
            margin: 0 auto;
        }

        form input, form select, form textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border-radius: 5px;
            border: 1px solid #ccc;
        }

        form button {
            background-color: var(--secondary-color);
